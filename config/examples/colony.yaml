name: acme-platform

integrations:
  discord:
    token: ${DISCORD_TOKEN}
    guild: ACME Engineering

defaults:
  poll_interval: 5m
  git:
    user_name: "Your Name"
    user_email: "you@example.com"
